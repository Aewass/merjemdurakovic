---
import CategoryPage from '../../components/CategoryPage.astro';
import { getStaticPaths as getCategoryStaticPaths } from '../../types/category';

// Re-export getStaticPaths for Astro's static site generation
export { getCategoryStaticPaths as getStaticPaths };

const { id } = Astro.params;
const { category } = Astro.props;
---

<CategoryPage id={id} category={category} />
